version: '3.8'
services:
  backend:
    image: nginx:alpine
    ports:
      - "8081:8081"
    volumes:
      - ./kanban-backend:/usr/share/nginx/html:ro

  frontend:
    build: ../kanban-frontend
    ports:
      - "8080:8080"
